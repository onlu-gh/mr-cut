'use client';
import {createTheme} from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {main: '#B87333'},
        secondary: {main: '#4F8864'},
        info: {main: '#878787'},
    }
});

export default theme;